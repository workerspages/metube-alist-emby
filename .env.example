# ===========================================
# MeTube + Alist + Emby 环境变量配置
# ===========================================
# 复制此文件为 .env 并根据需要修改

# 端口配置（Cloudflare HTTP 支持端口）
EMBY_PORT=8080
METUBE_PORT=8880
ALIST_PORT=2052

# 用户ID配置（运行 `id` 命令查看您的 UID/GID）
UID=1000
GID=1000
